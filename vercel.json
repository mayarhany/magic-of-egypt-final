{
  "framework": "angular",
  "buildCommand": "ng build --configuration=production",
  "outputDirectory": "dist/src/browser",
  "functions": {},
  "routes": [
    { "src": "/(.*)", "dest": "/" }
  ]
}